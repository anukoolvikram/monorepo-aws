name: Deploy to staging

on:
  push:
    branches:
      - main

jobs:
  redeploy_everything:
    runs-on: ubuntu-latest
    steps:

      - name: Prepare SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Add SSH host to known_hosts
        run: |
          ssh-keyscan -H 13.233.146.20 >> ~/.ssh/known_hosts

      - name: SSH & Deploy
        run: |
            ssh -i ~/.ssh/id_rsa ubuntu@13.233.146.20 << 'EOF'
            cd monorepo-aws/
            git pull
            export NVM_DIR=~/.nvm
            source ~/.nvm/nvm.sh
            echo "Installing Dependencies..."
            pnpm install
            pnpm run build
            pkill -f 'node .*fe-server' || true
            pnpm run start:fe-server
            pkill -f 'node .*http-server' || true
            pnpm run start:http-server
            pkill -f 'node .*ws-server' || true
            pnpm run start:ws-server
            EOF